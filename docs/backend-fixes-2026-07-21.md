# Бекенд: що фіксанути (QA-прогін 2026-07-21, dev)

Спостереження з живого прогону консолі проти `gba-api-dev` (сесія Manual QA: Товари + Облік/Платежі).

Виправлене раніше (за словами команди, 2026-07-21): 503 у `GET /products/get` через
нездоровʼя source-pricing і повільні відповіді `products/get` / `token/session` —
пункти зняті; переперевірити після стабілізації dev (на момент контрольної перевірки
о ~19:20 ендпоінт ще відповідав 502, ймовірно йшов деплой).

## 1. Назви типів цін зі змішаними розкладками (Medium)

- У source-pricing реальні назви містять латинські гомогліфи: «ЦP» — **латинська P (U+0050)**
  замість кириличної «Р», при кириличному «(НДС)».
- Фронт захистився нормалізацією гомогліфів (сортування пар ЦР/ЦО1/ЦО2), але
  правильно — щоб бекенд/джерело віддавали консистентну кирилицю.

## 2. Брудні довідники на dev (Low, дані а не код)

- Статті руху грошових коштів: сміттєві записи «234234», «тест»; частина назв російською
  («Оплата покупателя», «Оплата поставщику») при україномовній консолі.
- Банки: ЄДРПОУ з хвостовою комою («14360570,»), записи без МФО/ЄДРПОУ
  (КБ «Приватбанк» м. Хмельницький).
- Якщо dev-база використовується для приймання фіч — варто підчистити, інакше QA плутає
  дані з багами.

---

Довідково, що поправлено на фронті цим же днем (бекенду дій не треба):
- Баг 92: форма «Статті витрат» вбивала весь застосунок (deferred read
  `event.currentTarget` у функціональних апдейтерах) — виправлено у 18 файлах.
- Додано ErrorBoundary на рівні застосунку і кожного роуту, сплеш замість білого екрана
  під час відновлення сесії, error-хуки `createRoot`.
- Рапід-клік захист: дедуплікація ідентичних одночасних мутацій в apiClient.
