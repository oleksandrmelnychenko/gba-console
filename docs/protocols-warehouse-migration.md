# Protocols + Warehouse Migration (branch clients/full-migration)

Scope confirmed by user: migrate the two standalone product-adjacent legacy features that were still `todo` —
**Протоколи доставки товарів** (`product-delivery-protocols`, 4 routes) and **Склад Україна** (`warehouse/ukraine`, 2 routes).
Method: per-slice build → integrate → adversarial verify → tsc+eslint(+build+tests when tree is green) → commit.

## Status

### product-delivery-protocols
- ✅ `/product-delivery-protocols` — list: date/organization/supplier filters, load-more paging, export-document modal (POST `/delivery/product/protocol/print/documents`), create-protocol modal (POST `/delivery/product/protocol/new`, multipart), row-options modal navigating to logistic-path / specifications / product-income. Gate: tsc 0, eslint 0, build+tests green at build time.
- ✅ `/product-delivery-protocols/:id` — logistic path: protocol details card, status section (POST `/delivery/product/protocol/update/status`, label progression, GBA/!completed edit gate), invoices section + assign-invoices drawer (GET `/supplies/invoices/approved`, POST `/delivery/product/protocol/add/supply/invoices`), merged-services (create full-fidelity multipart `/supplies/services/merged/manage`, calculate `/supplies/services/merged/update/extra/charge`, remove `/supplies/services/merged/remove/before/calculated/gross/price`, assign-invoices `/supplies/services/merged/add/supply/invoices` + `/supplies/invoices/get/by/services`). Lookups: consumable supply services `/consumables/categories/supply/services/get?value=`, responsibles `/usermanagement/profiles/all/by?types=7`, supply organizations `/supplies/organizations/all`, currencies `/currencies/all`. Adversarial verify: 0 high-confidence correctness bugs.
  - Deferred (documented): the merged-service **edit** card omits legacy's task / information-task / act-providing-service delete-on-clear and re-create logic. The create path is full-fidelity; edit spreads `...service` so no nested data is lost (no correctness regression). Edit-side task/act mutation is the deferral.
- ✅ `/product-delivery-protocols/:id/specifications` — invoice/packing-list selector, EUR/UAH + УО/БО toggles, products+services grid with dynamic service columns + totals, upload specification (multipart `/supplies/invoices/specification/upload?invoiceNetId=`), upload delivery documents (`/supplies/invoices/documents/add`), merge invoices (`/delivery/product/protocol/merge/supply/invoices`, query repeated-key), download specification (`/supplies/packinglists/specification/get`). Adversarial verify: clean (deferred: per-row spec-code edit panel + Poland supplier-select panel). Gate: tsc 0, eslint 0.
- ✅ `/product-delivery-protocols/:id/product-income` — Ukraine placement flow: invoice→packing-list selector, placement dynamic-grid (mirrors warehouse order-placements editor; rows matched by PackingListPackageOrderItemId), ready-to-place (PATCH `/supplies/packinglists/item/readytoplaced/update`), CalculateVat (`/supplies/invoices/items/update/vat`), placement update (`/supplies/packinglists/placement/info/update`), create income (`/products/incomes/new/supply/ukraine/dynamic`), dynamic columns + move-remnants. Adversarial verify: fixed ready-to-place POST→PATCH + result-modal tab sync. Poland branch (PZ document, `/products/incomes/new/packinglist/dynamic`) deferred. Gate: tsc 0, eslint 0.

### warehouse-ukraine
- ✅ `/warehouse/ukraine` — tabbed shell with 6 tabs, permission-gated per tab (STORAGES_Ukraine_*). Built: Накладні/Інвойси (sales `GET /sales/all/filtered?status=Packaging&fromShipments=true`, print + act-protocol-edit print + TTN link), Замовлення Україна (`GET /supplies/ukraine/order/all/filtered?nonPlaced=true`, row → placements route, page-full Load-more), Реєстр накладних (`GET /sales/get/register/invoice`, print register), Звірка документів (`GET /history/order/item/get/verification`, export, multi-storage select), Підтвердження редагування (act/carrier editing counts + lists), Відвантаження (auto-shipments by transporter, editable QtyPlaces, edit delivery recipient/address/comment, carry-out IsSent=true, print creation page). Gate: tsc 0, eslint 0.
- ✅ `/warehouse/ukraine/orders/:id/placements` — order-placements dynamic-grid editor: fixed columns + dynamic dated placement columns with in-cell qty editing, placement sub-management drawer (storage/row/cell + qty), new column, delete column, move-remnants (dirty-guarded), save (POST full order) / cancel (reload). Validation messages match legacy. Gate: tsc 0, eslint 0.
- Adversarial verify (whole feature, 3-agent build): 0 crashes/wrong-payload bugs. Fixed: (1) per-tab permission gating restored to match legacy `PermissionCheck`; (2) orders Load-more switched to page-full semantics (independent of a backend total field). Accepted minor deviations (console-wide conventions): per-keystroke search (no 200ms debounce), shipments recipient/address editable even with a WarehousesShipment, TTN column falls back to CustomersOwnTtn outside the shipment branch.

## Endpoint uncertainties to verify against a live backend
- Warehouse list responses: total-count field name assumed `TotalRowsQty`/`TotalRowQty` with first-item fallback. Adjust normalizers if the gateway wraps lists differently.
- Proof-of-Editing qty endpoints: response assumed bare number or `{Qty|Total|Count}`.
- Document Verification storages: used `/storages/all` (legacy action) vs sibling console `/storages/get/all` — confirm backend expectation.

## Commit note
Route files (`lazyConsolePages.tsx`, `consoleRoutes.tsx`) currently interleave this migration's wiring with the user's concurrent WIP (accountable-expenses, advanced-reports, outgoing-cashflows, consumable-orders/-storages, payment-accounts/-expense-articles). Full `npm run build` is currently red due to that WIP (payment-accounts/types.ts TS enum; consumable-orders type errors), not this migration's code. Holding commit until a clean checkpoint / user direction on bundling.
