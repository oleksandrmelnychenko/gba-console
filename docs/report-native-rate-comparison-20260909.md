# Historical exchange-rate comparison in the console

Source19 selects one exact direct commercial or government rate definition and compares its recorded history at two independent dates. The constructor keeps kind, canonical decimal-string definition ID and both dates in versioned `RateComparison` settings. The existing native lookup route uses fields39/40 and at most50 results; generic period, grouping and filter controls are unavailable for this fixed report. Saved templates preserve exact IDs, option validation and selected measure order. Switching kind clears the selected definition; reset clears both date inputs and series search.

The report retains one exact currency-pair/series row even when both historical points are absent. Unknown values stay blank. Rates and absolute change display/export four decimal places; percentage change uses two. The console preserves the server's published values and does not recalculate differences from rounded cells. Source19 workbook/CSV recognition requires explicit requested dates, matching source attribution, history-point notes and coherent exact kind/currency/definition identity. It rejects duplicate rows, unknown fields, malformed precision and every subtotal/grand total. A filtered CSV may contain zero data rows while preserving attribution. Local event-date filters do not apply to this report.

The chart displays the report's one exact series without aggregating other series or pairs. Existing zero-inclusive axes and disconnected unknown line gaps remain in use. Neither table, chart nor CSV manufactures a total or average. Rates never reprice customer contracts or documents.

Capabilities come from the serialized C# dataset contract. Console checks cover reversed/equal dates, canonical IDs beyond JavaScript's safe integer range, malformed option aliases, templates, immutable API request snapshots, independent missing history, selected measure order, no-total files, precision, export and chart transformations. Real SQL, HTTP and browser acceptance belongs to the integration evidence. Console fixtures alone establish no source parity.

Limitations are explicit in the report: recorded dates have mixed provenance; current currency metadata may differ from historical metadata; retained history may be incomplete; source raw rates, multiplicity, population and permissions are not proved. Source parity remains false.

Final validation: the fresh affected suite passes221cases, including187source19cases, ten actual server XLSX binaries (four SQL-produced and six final core-writer exports), and23 independent Fraction-oracle display vectors. The broad application run recorded5,693passes and three negative API failures while final guard edits were in progress; all three pass in the fresh affected run. Deduplication gives5,696distinct cases with passing results across the accepted runs. This is not a claim that one broad run passed every case. Production build and changed-file lint pass; React Doctor retains one existing workspace-complexity warning and has no numerical score enabled. Root integration additionally exercised real HTTP, templates, downloads, browser presentation and CSV round trips.
