/** Canonical page-size choices for every list/registry page (the «Продажі» set). */
export const PAGINATOR_PAGE_SIZE_OPTIONS = ['20', '40', '60', '100', '500']
export const DEFAULT_PAGINATOR_PAGE_SIZE = 20
