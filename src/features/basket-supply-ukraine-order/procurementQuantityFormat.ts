/** Display fractional packs without whole-unit rounding or binary arithmetic tail digits. */
export const procurementQuantityFormat = new Intl.NumberFormat('uk-UA', { maximumSignificantDigits: 15 })
